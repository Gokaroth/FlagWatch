# Copernicus Marine credentials (free account at https://data.marine.copernicus.eu) — only needed if WMTS requires auth
COPERNICUS_USERNAME=
COPERNICUS_PASSWORD=
# Optional token if used instead of basic auth
COPERNICUS_TOKEN=
