# Dependencies
node_modules/

# Local env / secrets (Copernicus creds, etc.) — NEVER commit
.env
.env.*
!.env.example

# Local server state (snapshot + condition history on the Fly volume; dev fallback dir)
.state/

# Netlify local (legacy — backend migrated to Fly.io)
.netlify/
deno.lock

# OS / editor cruft
.DS_Store
*.log

# Local backups / scratch
*.backup
*.backup2
*.test
