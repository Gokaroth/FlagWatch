# Dependencies
node_modules/

# Local env / secrets (Copernicus creds, etc.) — NEVER commit
.env
.env.*
!.env.example

# Netlify local
.netlify/
deno.lock

# OS / editor cruft
.DS_Store
*.log

# Local backups / scratch
*.backup
*.backup2
*.test
